{
  "name": "inventory-management-system",
  "version": "1.0.0",
  "description": "Sistema interno de gestión de inventario para uso empresarial",
  "main": "index.html",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "build": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "build:css": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "watch": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "audit": "npm audit --audit-level=moderate",
    "deploy": "vercel --prod",
    "preview": "vercel"
  },
  "devDependencies": {
    "vercel": "^32.0.0",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  },
  "engines": {
    "node": "22.x"
  }
}
